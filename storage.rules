rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
  	match /assets {
    	allow read;
    }
  	match /avatar {
    	allow read;
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
